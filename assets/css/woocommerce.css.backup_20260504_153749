/* DKG clean WooCommerce layout */

/* Product grid */
.woocommerce ul.products {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 2rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.woocommerce ul.products li.product {
  width: auto !important;
  margin: 0 !important;
  float: none !important;
  padding: 14px !important;
  border-radius: 22px;
  background: rgba(255,255,255,0.07);
  border: 1px solid rgba(255,255,255,0.12);
  overflow: hidden;
}

.woocommerce ul.products li.product a img {
  width: 100%;
  height: auto;
  display: block;
  border-radius: 16px;
}

.woocommerce ul.products li.product .woocommerce-loop-product__title {
  color: #fff;
}

.woocommerce ul.products li.product .price {
  color: rgba(255,255,255,0.85);
}

.woocommerce ul.products li.product .button {
  display: inline-block;
  margin-top: 0.75rem;
}

/* Single product */
.single-product div.product {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
}

/* Full-page dynamic shop background */
body {
  background-color: #000;
}

.dkg-real-shop-bg {
  position: fixed !important;
  inset: 0 !important;
  z-index: 0 !important;
  background-size: cover !important;
  background-position: center !important;
  background-repeat: no-repeat !important;
  opacity: 0.65 !important;
  pointer-events: none !important;
}

.dkg-real-shop-bg-overlay {
  position: fixed !important;
  inset: 0 !important;
  z-index: 1 !important;
  background: rgba(0,0,0,0.48) !important;
  pointer-events: none !important;
}

/* Keep actual page above background */
.site-header,
.dkg-shop-main,
.site-footer {
  position: relative !important;
  z-index: 2 !important;
}

/* Shop content wrapper */
.dkg-shop-main {
  width: 100%;
  max-width: 100%;
  margin: 0 auto;
  padding: 70px 20px 110px;
  color: #fff;
}

.dkg-shop-plate {
  width: min(1180px, calc(100vw - 80px));
  margin: 0 auto 70px;
  padding: 34px;
  border: 2px solid rgba(255,255,255,0.22);
  border-radius: 34px;
  background: rgba(20,20,20,0.72);
  box-shadow: 0 24px 80px rgba(0,0,0,0.45);
  backdrop-filter: blur(2px);
}

.dkg-shop-plate h1,
.dkg-shop-plate h2 {
  margin: 0 0 28px;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: #fff;
}

.dkg-featured-collection {
  border-color: rgba(255,255,255,0.38);
}

.dkg-more-products h2 {
  margin-top: 4px;
}

/* Disable old pseudo backgrounds if any browser cached old rules */
.dkg-shop-main::before,
.dkg-shop-main::after {
  content: none !important;
  display: none !important;
}

/* Mobile */
@media (max-width: 900px) {
  .woocommerce ul.products {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .single-product div.product {
    grid-template-columns: 1fr;
  }

  .dkg-shop-plate {
    width: min(100%, calc(100vw - 36px));
    padding: 22px;
    border-radius: 26px;
  }

  .dkg-shop-main {
    padding: 38px 0 80px;
  }
}
